appId: app.cuetracker
---
- tapOn: ${TAB_LABEL}
- assertVisible: ${TAB_LABEL}
- runFlow:
    when:
      true: ${TAB_LABEL == "Up Next"}
    commands:
      - assertVisible:
          id: "marquee-backdrop"
      - assertVisible:
          id: "marquee-mark"

# `--mode mock`: the app runs against the local fake Trakt (pnpm mock:trakt), no
# real account and no network. The client id is never checked by the mock.
VITE_TRAKT_CLIENT_ID=mock-client
VITE_TRAKT_API_BASE=http://127.0.0.1:8787
